services:
  devops:
    build: .
    image: devops:latest
    restart: always
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-server:5432/ObDb
    networks:
      - OBNet

networks:
  OBNet:
    external: true

#> docker compose up --build -d